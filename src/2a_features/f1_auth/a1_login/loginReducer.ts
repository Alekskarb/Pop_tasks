export type StateType = Array<string>;
export type ActionType = Array<string>;


let initialState:StateType = [];

const loginReducer = (state = initialState, action:ActionType) => {
    return state;
};
export default loginReducer;